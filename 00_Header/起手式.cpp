#include <bits/stdc++.h>

using ll = long long;

#define de(x)   std::cerr << "  debug  " << #x << "  ==  " << x << std::endl;

void solve () {
    
}

int main () {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr), std::cout.tie(nullptr);
    int Test = 1;
    std::cin >> Test;
    while (Test --) solve ();
    return 0;
}